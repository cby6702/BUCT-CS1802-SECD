apply plugin: 'com.android.application'

android {
    signingConfigs {
        debug {
            storeFile file('D:\\cnm\\BUCT-CS1802-SECD\\app\\BaiDuMapSign_1802')
            //大家这里的绝对路径需要自己找到BaiDuMapSign_1802文件的地点然后更改
            storePassword 'wwang123'
            keyAlias = 'wangnaihao'
            keyPassword 'wwang123'
        }
    }
    compileSdkVersion 29
    buildToolsVersion "29.0.3"

    defaultConfig {
        applicationId "com.example.baidumapdemo"
        minSdkVersion 26
        targetSdkVersion 29
        versionCode 1
        versionName "1.0"
        multiDexEnabled true
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.2.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.0.4'
    testImplementation 'junit:junit:4.12'
    androidTestImplementation 'androidx.test.ext:junit:1.1.2'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.3.0'
    implementation files('libs/BaiduLBS_Android.jar')
    implementation files('libs/gson-2.8.6.jar')

    //邹傲 首页需要的组件依赖
    implementation 'com.android.support:design:28.0.0'
    implementation 'com.zaaach:citypicker:2.0.1'
    //焦春雨 登陆界面需要的组件依赖
    implementation 'com.squareup.okhttp3:okhttp:3.4.1'
    debugImplementation 'com.squareup.okhttp3:logging-interceptor:3.4.1'

    android {
        ndkVersion "22.1.7171670"
    }
}
